import StoreProducts from "./StoreProducts";
const Store = () => {
  return (
    <>
      <StoreProducts />
      {/*   {products.map((item) => (
        <StoreProducts {...item} />
      ))}
        */}
    </>
  );
};
export default Store;
